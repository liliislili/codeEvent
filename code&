#include <stdio.h>
#include <stdlib.h>//error in library

int main() {
    int number = 7;
    int guess;

    printf("Guess the number (between 1 and 10): ");
    scanf("%d", guess);  // BUG: Missing & before guess

    if (guess = number) {  // BUG: Using = instead of ==
        printf("You guessed it right!\n");
    } else {
        printf("Wrong guess. Try again next time.\n");
    }

    return 0;
}
